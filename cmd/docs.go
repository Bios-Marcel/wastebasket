/// This packages contains the main functions for executables and their
/// concrete implementations. The reason for having separate root commands, is
/// that you can have both binaries that only do one thing and one big binary
/// that has all the other root commands as children.

package cmd
